var replaceElements = function(arr) {
    
for(let i=0;i<arr.length;i++){
    arr[i]=0
   for(let j=i+1;j<arr.length;j++){
       if(arr[j]>arr[i]){
           arr[i]=arr[j]
       }
   }
    
}
    arr[arr.length-1]=-1
   return (arr)
};
