/**
 * @param {string} s
 * @return {string}
 */
var freqAlphabets = function(s) {

        s=s.replace(/([\d]{2})#/g,(match,p1) => {
  return String.fromCharCode(parseInt(p1)+96)
})
      s=s.replace(/([\d]{1})/g,(match,p1) => {
  return String.fromCharCode(parseInt(p1)+96)
  })

    return s
};
